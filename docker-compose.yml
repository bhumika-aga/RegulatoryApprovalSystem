version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: regulatory-app
    environment:
      JWT_SECRET: ${JWT_SECRET:-dGhpcy1pcy1hLXZlcnktbG9uZy1zZWNyZXQta2V5LWZvci1qd3QtdG9rZW4tc2lnbmluZy1hdC1sZWFzdC01MTItYml0cw==}
      CAMUNDA_ADMIN_PASSWORD: ${CAMUNDA_ADMIN_PASSWORD:-admin}
    ports:
      - "8080:8080"
    networks:
      - regulatory-network

networks:
  regulatory-network:
    driver: bridge
